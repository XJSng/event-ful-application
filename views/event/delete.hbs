{{#extends "base"}}
{{#block "title"}}
Delete {{event.title}}
{{/block}}


{{#block "content"}}
<div class="mt-2 alert alert-danger">
    Are you sure you want to delete this event: {{event.title}}?
    <div>
        <form method="POST">
            <button class="btn btn-danger">Yes</button>
            <a href="/event" class="btn btn-success">No</a>
        </form>
    </div>
</div>
{{/block}}

{{/extends}}